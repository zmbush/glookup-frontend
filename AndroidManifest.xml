<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="zipcodeman.glookup"
      android:versionName="1.08" android:versionCode="12">
    <uses-sdk android:targetSdkVersion="8" android:minSdkVersion="8"/>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"></uses-permission>
    <!-- <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS"/> -->

    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <service android:name=".GlookupUpdateService" />
        <activity android:name=".GlookupFrontendActivity"
                  android:label="@string/app_name" android:configChanges="keyboard|keyboardHidden|orientation|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".GlookupBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        
        <receiver android:name=".GlookupAlarmReceiver" />
        
        <activity android:name="AddUserActivity" android:launchMode="standard"></activity>
        <activity android:name="MainGradesActivity" android:configChanges="keyboard|keyboardHidden|orientation|screenLayout"></activity>
        <activity android:name="SubGradeActivity" android:configChanges="keyboard|keyboardHidden|orientation|screenLayout"></activity>
        <activity android:name="org.achartengine.GraphicalActivity">
        </activity>

    </application>
</manifest>